# IYPT-Skill

一套 Claude Code skill 流水线，让 Claude 按序完整攻下一道 IYPT 题。远端：https://github.com/Alumin-Hydro/IYPT-Skill（`main`，git 凭据已缓存可直接 push；本机**没有** gh CLI）。

```
Skill 1 (物理分析) ⇄ 审稿  →  Skill 2 (仿真/验证/可视化) ⇄ Skill 4 (美学审查)  →  Skill 3 (幻灯片) ⇄ Skill 4
                       ▲                    │
                       └────────────────────┘  反向边：数值打脸模型时回送修订（docs/pipeline.md §5）
```

| Skill | 名称 | 状态 |
|---|---|---|
| 1 | `iypt-analysis` | ✅ |
| 1R | `iypt-physics-review` | ✅（17 条错误模式；**P17「判据界错了量」产出比最高**） |
| 2 | `iypt-simulation` | ✅（六道验证门 + 中间量验证） |
| 3 | `iypt-slides` | ✅（**溯源即结构**：数字不许手打，只许写指针） |
| 4 | `iypt-design-review` | ✅（16 条设计失败模式；**HTML 也是图**） |

**四个 skill 都做完了。** 接下来的工作是**在新题目上跑**，把踩到的坑回填进 skill（见下面的铁律）。

## 动手前先读

- **`docs/pipeline.md`** —— 工作区约定、四个 skill 的交接契约、**反向边的五种 status**，**单一事实源**。改产出格式先改它。
- **`skills/iypt-analysis/templates/model-spec.schema.json`** —— Skill 1 → Skill 2 的接口。
- **`skills/iypt-simulation/templates/results.schema.json`** —— Skill 2 → Skill 3 的接口。
- **`examples/magnetic-brake/`** —— 一次真实跑通的完整产出，回归基线。**它把反向边完整走了一遍**：
  r1 运行判 `MODEL-CHALLENGED`（快照 `02-sim/results-r1.json` + `model-challenge-r1.md`）→ Skill 1 按
  **物理理由**修订（r2 修 3 处 SPEC-DEFECT；r3 修 A-2 的一阶系数漏了个 2）→ 重跑，现在是
  `PRESCRIBED-REVISION`（两条 RISKY 假设都**如预期地**不成立 —— 模型边界被正确定位）。
  **归档链 `model-spec-r1/r2.json` 是 P16（事后合理化）审稿的物证。**

## ★ 开发 skill 时的第一条铁律：**改 skill，不要只改这道题**

跑例子时踩到坑，**先问一句**：

> **这个坑是这道题特有的，还是任何题都会踩的？**

是后者 —— **改 skill，不许只在例子里修好就算完。**

**为什么这条必须写在最前面**：实际场景是**一个没有这段开发上下文的 Opus 4.8 + skill**。它不知道你在 magnetic-brake 里踩过什么、修过什么。**你在例子里修好而没回填进 skill 的东西，等于没修——下一道题它会一模一样地再踩一遍。**

每个坑要走完这三步：

1. **例子里修好**（证明修法可行）
2. **回填进 skill**：改 `SKILL.md` / `references/` / `templates/` / schema
3. **加机械检查**（能查就查）—— 写进 `check_analysis.py` / `check_sim.py`。
   > 文档里的劝诫会被忽略；**机械检查不会**。

## 从 13 份真实 IYPT 报告里学到的（`example-ppt/`）

6. **题目任务是「挖」出来的，不是「读」出来的。** 题面里每个限定词都是一条任务的种子：
   `"under certain conditions"` → **条件边界**（答案是**参数空间相图**，不是一条曲线）；
   `"can also exhibit other interesting behaviour"` → **模式分类**（→ 混沌、Lyapunov）。
   **而「混沌」两个字，题面里一个都没有。** 5/13 份报告有专门的「题目任务」页。
7. **因变量有三层，第三层「模式」是分水岭。** 标量 / 函数 / **模式**（周期-准周期-混沌、
   加速-减速-卡住、单稳-多稳）。新手把 "study the movement" 理解成「测 x(t) 然后拟合」。
8. **一句话的物理本质决定图的骨架。** 「磁势能向动能的**非对称转换**」⇒ 必须画势能景观；
   「**非线性多体耦合**」⇒ 必须画相图/频谱/Lyapunov。**说不出本质 = 不知道该画什么图。**
9. **仿真要验证「中间量」，不是验证最终结果。** 真题的做法是**拿高斯计去测 B 场**（链条中间
   的量），不是拿末速度去反证模型。**两个错误可以互相抵消。** 5/13 份报告有专门的模型验证节。

## 已经吃过的亏（别再踩）

1. **零号规则**：验证类 agent 会拿"自己重推的正确版本"替换掉"作者写的字"，从而对公然的错误失明。对策：**逐字抄写**被验对象的原文，并**机械校验引文是原文的子串**（`check_sim.py` 的 `quoted_expectation` 检查）。
2. **`must_not` 必须查「结构」，不能只查「拟合出来的数」。** 只少一个修正的 bug，会让拟合值落在"陷阱值"和"真值"**之间**，从两条断言中间溜走。要找那些在正确/退化模型下**离散地不同**的量（峰位、节点数、对称性）。
3. **收敛门要在扫描端点上做**，不能只在基准点做。绝对截断长度会在扫描的一端悄悄失效，而**斜率已经错了**。
4. **修订必须传播到「结论」和「契约」，不能只落在「推导」上。** 实测：审稿抓到的一个错误，修订只改了正文的推导，**漏掉了预测表和 model-spec.json**——而那两处恰恰是下游真正读的。**目前没有机械检查能发现这种脱钩。**
5. **`SPEC-DEFECT` 是个逃生舱，门槛必须画死**：**要引用仿真数字才能说明"契约写错了"的，一律不是 SPEC-DEFECT**，而是模型被数据打脸了。

6. **非空 ≠ 有效。凡是 id 之间的引用，都必须解析一遍。** 实测：`figures[].assertion_ids` 曾经是编出来的 `AS-V1`…`AS-V4`，四个 id 一个都不存在 —— 而 `FIG-NOASSERT` 只查「非空」，**一个编出来的 id 是非空的**。它顺带掩盖了「V-4 一条真断言都没有」这个更糟的事实。

7. **契约必须自足，否则下游只能去别处找补、或者手打。** 实测：做 PPT 时才发现 `results.json` 里**没有参数** —— 而第一页「问题设定」要写 `a = 6 mm`。于是 Skill 3 要么去读 model-spec（「每个数字都能追回 results.json」当场破功），要么手打。**两条都是谎的种子。**（现在 `parameters`/`essence`/`assumptions` 强制透传。）

8. **「名字出现」不等于「结论被汇报」。** `FALSIFIED-DROPPED` 的第一版查的是「A-1 这个名字有没有在 deck 里出现过」—— 然后冒烟测试**删掉了整个「模型边界」节**，它照样通过了。因为 **A-1 是一条假设的名字，它在理论页的台账里本来就会出现**。现在要求：被证伪的断言**必须以 `verdict` 块出现**（那种形式无法被措辞软化）。

9. **渲染不出来的产出，等于没有被审过。** `02-sim/interactive/*.html` 上两个峰值标注**叠印成一团糊字**，而那两个数正是那张图的全部结论。**这个 bug 活了很久 —— 不是没人想审，是根本渲染不出来。** 现在有 `render_html.py`（headless Edge/Chrome）。**HTML 也是图。**

### ★ electrical-damping（第二道题）压出来的 —— 这些坑 magnetic-brake 一个都没暴露

10. **★★ 判据可以「形式上完全合格、实质上错 2000 倍」。** 这是这轮最贵的一课，已固化为审稿模式 **P17**：
    - **判「耗散可忽略」时用了长度比。** 原文：「$\delta = 38$ mm $\gg d_{\rm wire}=0.4$ mm ⟹ 涡流 $<10^{-4}$，忽略」——
      **$\delta\gg d$ 恰恰是涡流「充分发展」的条件，不是它小的条件。** 反例就在本仓库里（magnetic-brake 的铜管）。
      真值是开路本底的 **24%**。**要判耗散，唯一合法的判据是功率比或力比。**
    - **一阶判据被用在极值点上。** $\lvert G'(z_0)\rvert A/\lvert G(z_0)\rvert \ll 1$ 用在 $\lvert G\rvert$ 的**峰**上 ⟹
      $G'(z_{\rm pk})\equiv0$ ⟹ **分子恒为零 ⟹ 对任何振幅都报「线性 ✓」**（真实误差从 0.7% 长到 31%）。
      **判据必须无关阶数**：$\nu = \max_{|u|\le A}\lvert f(x_0+u)-f(x_0)\rvert/\lvert f(x_0)\rvert$。
    - **自检一句话**：**一条 RISKY 假设，判据算出来是 $10^{-8}$ —— RISKY 的定义是「勉强满足」。**
      **要么分级错了，要么判据测的不是它该测的东西。**
    - **⇒ 最后一问：这条判据被写进契约的哪里？** 只在台账里 = MINOR；
      **同时是某张图的区域边界或某道门的阈值 = MAJOR**（下游会按错的规矩执行 —— 实测：它让 F-4 和 F-5 在同一个点上给出**相反**的断言）。
    - **`check_analysis.py` 对这一条永远是零 ERROR。洞在物理里，不在语法里 —— 这就是对抗式审稿存在的全部理由。**

11. **门的「容差」本身也要在扫描端点上定。**（教训 3 的升级版。）Gate 0c 的容差 $<0.5\%$ 是在基准点定的（误差 0.01%），
    而扫描上端的真实误差是 **0.46%** —— **贴着门框过**。下游会撞上一个**伪失败**，然后去「修」一段本来正确的代码。

12. **正则写死一种排版 = 在契约里偷偷加一条没人知道的格式规定。**（这个病犯了三次。）
    `**S-1**`（加粗 ID）→ 假 `NO-SPEC-SHEET`；`### A-6a`（**修订把一条假设拆成两条**）→ 旧正则抠出 `A-6`，
    **两个方向同时报 ERROR，一次正确的修订被当成三个错误骂回来**；`| **S-8** ★ |`（ID 后带装饰）→ **整行隐形**。
    **而「拆假设」正是修订最常做的动作。** 全部钉进 `--selftest`。

13. **「这一项不可忽略」是全文最负责任的一句话 —— 不许骂它。**（同一个病第三次：正则看得见关键词，看不见否定。）
    这次被误报的是「**它不是「可忽略」的**」。**但修误报时绝不能把桥放宽成 `[^。]{0,8}`** ——
    那样「这一项很小，不影响结论，忽略」也会被放行，而那正是这条检查要抓的东西。
    **（教训 11 的另一面：为消误报而削弱检查，是最贵的错。）正反两向都钉进 `--selftest`。**

14. **审稿给的修法可能比原版更强 —— 那时要接受它，并说清「结论侥幸对了，但论证是错的」。**
    实测：P2 的标定**侥幸存活**（非线性被小振幅段平均掉了），**但作者给的理由是错的**。
    **「结论对」和「论证对」是两件事，必须分开说。**

15. **★★★ 最贵的一条：拆掉一把失明的锁之后，你换上的新锁多半也是瞎的。**

    r1 有一条判据在极值点结构性失明 → 我抓到了，把它写成了审稿模式 **P17** →
    **然后我拆掉那把锁，换上了三把新的**，还报了 $R^2 = 1.000000$、斜率偏差 $+0.0\%$。
    **r2 审稿证明：三把新锁全是瞎的。**

    | 我以为「变强」了 | 实测 |
    |---|---|
    | 抛物线的「顶点 $=\gamma_{\rm oc}$」 | **完美对中的正确模型偏 $+17.5\%$ ⟹ 判死**（误差棒的 8.8 倍）。拟合区间 ±6 mm 是**随手画的** |
    | 「$R^2 = 1.000000$，斜率偏 $+0.0\%$」 | **75% 的拟合点落在「效应已经自己关掉」的尾巴上** ⟹ **把要测的系数改错 30%，全过** |
    | 「常数阻尼 ⟹ 截距 $\equiv 0$」 | **捏造的稻草人** —— 变换用的是**我自己预言的** $q$，代进对方模型 ⟹ **截距完全相同** |

    **为什么三把一起瞎？因为我只跑了一个方向** —— 只验证了「新判据在**正确模型**上通过」，
    **从没验证「它在**错的模型**上会不会照样通过」。**

    > **一条判据，只在正确模型上跑过 —— 那不叫验证，那叫「换了一把新的失明的锁」。**

    **⇒ 已固化成一道机械门**（`Stage 8.5` + `criterion_matrix`）：
    - `01-criteria/criterion_matrix.py` → `matrix.json` → 契约的 `criterion_matrix`（**schema required**）
    - **←** 正确模型全 PASS（`CRIT-FALSEKILL`）· **↑** 每个错模型被抓（`CRIT-MODEL-UNCAUGHT`）
      · **→** 每条判据抓到东西（`CRIT-BLIND`）
    - **错模型必须带 `why_a_student_writes_it`** —— **说不出「为什么有人会这么写」，那就是稻草人。**
    - **★ 必须造一个「形状全对、只多一个常数偏置」的错模型** ——
      它的标度律一字不差，**只有绝对值偏了**。**能不能抓到它，是判据集是否完备的试金石。**
    - **警惕信号**：一条判据报出 $R^2 = 1.000000$。**「太好了」本身就可疑** ——
      **去问：这个拟合的杠杆落在哪一段数据上？**

16. **「修订有没有传播」现在有机械检查了**（教训 4 说的那个「没有」）。
    归档链 `model-spec-r{n}.json` 是强制的 ⟹ **旧值有物证** ⟹ 拿旧值 grep 新文档：
    `STALE-VALUE`（值变了但旧值还在）+ `STALE-SYMBOL`（改名后的孤儿符号）。
    **实测：一次审稿抓出 13 处 r1 旧值还活着**，包括 `(26).latex` 里**同一个方程 LHS 用 $M$、
    阻尼项用 $M_{\rm eff}$**。**豁免规则：引用旧值必须点名版本**（行内 / 上文三行 / 节标题）。

17. **一个「设计结论」也可以是口头断言。** 我写「换利兹线：总铜量不变 ⟹ $R_c$、$G$、$\beta$ 全不变」——
    **没算。** 真算：利兹线束粗 22% ⟹ 线圈涨大 ⟹ $\beta$ 掉 23% ⟹ **窗口反而更差**。
    正确的杠杆是一条标度律（$\beta\propto Nd^2$ 不变，而 $\gamma_{\rm eddy}\propto Nd^4$）——
    **换更细的实心线，不是利兹线。**
    **这和 P2（口头忽略）、P17（判据界错了量）是同一个病，只是它出现在「工程建议」里。**

### ★★★ r3 审稿压出来的 —— 四条，**每一条指的都是我自己建的那把锁**

18. **★★★ 我在这个文件里手打了一个数字，而它是编的。**
    教训 15 的表格里写着「r2 审稿实测：正确模型偏 $+27.7\%$」——
    **`01-review-r2.md` 里 "27.7" 出现 0 次。它逐字写的是 $17.5\%$（出现 8 次）。**
    那个凭记忆填出来的数，从例子一路长进了 **`CLAUDE.md`、`iypt-analysis/SKILL.md`、
    `physics-failure-patterns.md`（审稿人的弹药库）、`check_analysis.py` 的错误消息、和 memory**。
    > **零号规则通常说：审稿人不许拿自己的推导替换作者的字。**
    > **这是它的镜像：作者不许拿自己的记忆替换审稿人的字。**

    **而这正是 Skill 3 的核心（「数字不许手打，只许写指针」）—— 我在写规矩的那份文件里，违反了那条规矩。**
    **⟹ 机械门 `REVIEW-CITE-GHOST`**：凡是把一个数归给「r{n} 审稿」的，
    **那个数必须能在 `01-review-r{n}.md` 里逐字 grep 到**（除非在附近点名了别的来源）。
    **编出来的数字，不可能是原文的子串。**

19. **★★★ 机械门也会瞎 —— 而且瞎法和物理判据一模一样。**（教训 15 的 meta 版本。）
    我上一轮建的 `STALE-VALUE`（教训 16），第一行是 `if abs(ov-nv) <= 1e-12: continue` ——
    **值没变就跳过。**
    > **而「忘了改」的定义，就是「值没变」。**

    实测：`targets[\gamma].baseline_value = 2.5978` 在 **r1/r2/r3 三代一字未动**，
    真值 **2.3454**（偏 +10.8%）。r2 审稿逐字点过它，r3 的修订记录写着「全部清掉」——
    **而两道 STALE-\* 门都没响。**
    **⟹ 三道新门**（`SPEC-SELFCONTRADICT` / `TARGET-NO-RECIPE` / `CLOSED-FORM-ORPHAN`）：
    **每个 `baseline_value` 必须能由它自己的 `closed_form` 复算出来**，
    算不出来就必须写 `numerical_recipe` 显式声明「它没有闭式」。**不许沉默地跳过。**

    **★ 而我写这三道门时，第一版又瞎了一次**：`CLOSED-FORM-ORPHAN` 把 `1/(gamma - gamma0) = …`
    的**左边**当成了「定义」⟹ **`gamma0` 把自己定义了 ⟹ 门对它彻底失明**。
    **我差一点就交付了一把新的瞎锁。**（已钉进 `--selftest` 的反向用例。）

20. **★★ 双向表（Stage 8.5）是一份**自评** —— 它可以被调到全绿。**（审稿模式 **P18**。）
    四道 CRIT 门查的全是**布尔值**：正确模型 PASS 吗、每个错模型被抓了吗、每条判据抓到东西了吗。
    **它们看不见「抓得有多勉强」，也看不见「容差从哪来」。**
    - **容差是作者定的**：源码里 `< 0.12` / `< 0.20` / `< 0.15` —— 裸数字，与正文的 15% **矛盾**。
    - **★ 错模型的幅度也是作者挑的**：`bug-C` 设成「$\beta$ 错 **+30%**」（被 12% 的容差抓到 ✓），
      **而作者自己在 `why_a_student_writes_it` 里写着「标称 $B_r$ 公差 ±5% ⟹ b 偏 ±10%」——
      10% < 12% ⟹ 他亲手描述的那个真实场景，五条判据一条都抓不到。**
      **⟹ 要扫幅度、报「最小可检测幅度」$\varepsilon^*$。「bug-C 被抓到了」没有信息量。**
    - **「正确模型」必须是契约命令 Skill 2 去积的那个模型**（表用 Model-0，契约用 Model-2 ⟹ $c_2$ 差 4.3%）。
    - **「不误杀」必须在协议自己承认的系统误差上跑**（`crit_P3` 钉死 `z0=0.0`，
      而 §9 写着「不要试图对准中心」⟹ 偏心 0.17 mm 就判死正确模型。**同一个病第三次**）。
    - **「被抓到」必须是判据的字面逻辑抓到的**（naive-A 的「被五条抓到」，明细全是「提不出 Γ」——
      **一个记账截断，记了五次**）。

    **⟹ P18 现在有机械门了**（`check_analysis.py`，全部钉进 `--selftest`）：
    `CRIT-TOLERANCE-UNSOURCED`（容差是裸数字）· `CRIT-NO-ROBUSTNESS`（不误杀只在理想点上跑过）·
    `CRIT-NO-MIN-DETECTABLE`（没扫 ε\*）· `CRIT-MODEL-UNCAUGHT` 现在**不认 DEGENERATE**。
    **两个例子都升级了**（`criterion_matrix.py` → `robustness_scan` + `min_detectable` + `tolerance_source`）。

    > **★ 一个副产品**：`ε*` 是**可汇报的物理结论** ——
    > 「我们的判据能分辨 β 的 **13%** 偏差」是一句真话，而「bug-C 被抓到了」不是。
    > 它还直接长出一条**实验建议**：**必须用高斯计实测 $G'(0)$**，
    > 否则预言侧的 ±10% 会把 P1c 的容差吃光 —— **判据本身已经到头了。**

21. **★★ 审稿人（Skill 1R）本身也是一把锁 —— 而它是全流水线唯一从没被验过的那把。**
    Skill 1/2/3 都有 `--selftest` / `smoke_test.py`（注入 bug 看抓不抓得到）。**1R：零。**
    **这一轮验了它**：送审前我把独立观察清单**预注册**在 scratchpad（**连同「我预言它会漏哪几条」**），
    审稿回来再对账。
    **结果：它比我强** —— 我预言它会漏掉「表盲于预言侧的错误」，
    **它不但抓到了，物证还比我硬**（我假想的是 `M_eff` 漏弹簧 `/3`；它找到的是
    「双向表的『正确模型』根本不是契约命令下游去积的那个模型」）。
    > **★ 预注册必须在看到报告**之前**落盘。**
    > **看到报告后再说「我早就发现了」—— 那正是 P16（事后合理化）本身。**

### ★★★ r5 审稿压出来的 —— 我为补 r4 新造的两道机械门，两道都**结构性失明**（元模式第六次复发）

22. **★★★ 一道门的 `--selftest` 本身也是一份自评（P18 再上一层楼）。**
    r5 我造了两道门（`CRIT-ROBUSTNESS-COARSE` 查「δ 边界是否二分定出」、`PROSE-FORMULA-GHOST` 查「散文公式幽灵」），
    `--selftest` **全绿**、两个例子 **0 ERROR**。**r5 审稿一构造就穿：**
    - `delta_max=None` ⟹ 门挂在 `elif delta_max is number` 上，**整道门被静默跳过**（而「扫描范围太小」恰好产出 None）；
    - **手写一个窄括号**（把 r4 判死的虚报值 0.17 配上 `[0.169,0.170]`）⟹ 门只查三数自洽，**证不了它是二分跑的，照样放行**；
    - `|G'(0)| 公式算出来 999.99`（非 target）、`targets[γ] = 9.9999`（裸「=」）⟹ 幽灵门只认 H2 事故的确切句式，全溜。
    > **★★ 最疼：我的 `none_ok` 用例把「None ⟹ 放行」亲手钉成了「预期行为」，给盲区盖了章 —— 而我是带着这条元模式满脑子写的。**
    - **⇒ 根因**：selftest 只跑了「该抓的→抓到、诚实的→放行」，**从没跑「它号称要挡、但换个形状→该抓却放行」。**
    - **⇒ 收敛的修法（不是无限打补丁）**：**① 每道门的 selftest 必须带「盲区探针」**——造出让门失明的输入；**造不出 = 你还没理解它守的是什么。**
      **② 门要对自己的 scope 诚实**：修不了的一类（如「非 spec 量的幽灵」「provenance 靠重跑保证」）**明写「已知局限」**，探针断言它放行并注明为什么，**不假装门覆盖了它**（诚实 scope 胜过虚假安全感）。
    - **⇒ 已机械化**：`check_analysis.py` 文件头「★ 盲区探针」纪律；两道门的 `selftest()` 后各跟一组探针（能修的翻成「抓到」，修不了的记录为「已知局限」）；`robustness_scan` 新增必填 `scan_upper_bound`（让 `delta_max=None` 可证伪）。
    - **⇒ 顺带**：revise 前必须先归档 `model-spec-r{n}.json`（r5 漏了一次，我自己违反了「归档强制」教训 16）。

23. **★★★ 「盲区探针」本身也不完备 —— 这是七次复发的最后一层，也是这一整段的收敛点（r6 审稿）。**
    我给两道门加了盲区探针（教训 22），以为收敛了。**r6 审稿又构造出两个盲区，两个都不在探针集里**：
    - **★ 真 MAJOR：门读的是 `model-spec.json` 里**内嵌**的 `criterion_matrix`（手拷副本），从不读 `matrix.json`。** 而我 r6 那句「provenance 靠重跑 matrix.json 保证」是**空标签** —— 重跑碰不到门消费的数据。把 r4 判死的 `delta_max=0.17` 篡改进内嵌、matrix.json 不动，**全套 CRIT 门 0 ERROR**。（r7 补 `CRIT-MATRIX-DESYNC`：内嵌逐字 == matrix.json。）
    - `PROSE` 门措辞表外的词（`计算得/等于/求得`）仍漏 —— 措辞表扩几个词只是「同一个病换更大的有限数」。
    > **「列几个探针」= r3 那张「全绿表」在探针层的翻版 —— 它证明不了自己穷尽了。这是 P18「你怎么知道判据集完备」的同一个未解问题，又升了一层楼。**
    - **⇒ 机械门（含它的盲区探针）无法机械地证明自己完备。收敛不靠更完美的门，靠**流程**：**
      **① 探针挡**已知**盲区**（写死 selftest，不 regress）；**② 对抗审稿挖**未知**盲区**（**每改 / 每加一道门 → 派一次 fresh `iypt-physics-review`**，让它专门构造「让这道门失明」的输入）。**缺一不可。**
    - **⇒ 已机械化（r7）**：`CRIT-MATRIX-DESYNC`（内嵌 == matrix.json，等式不是启发式）；`SKILL.md` 的假描述改掉；`robustness_scan` 加 `systematic_error_budget` 给 `scan_upper_bound` 上下限；措辞表明写为**启发式**（不再扩词表，靠对抗审稿兜底）。
    - **★ 诚实的边界**：`CRIT-MATRIX-DESYNC` 只堵「改内嵌一处」；「内嵌+matrix.json 两处一起改」仍能骗过 —— 那靠「提交前重跑 `criterion_matrix.py`」+ 对抗审稿。**每一道机械门都有这样一条边界，写出来，别假装没有。**

10. **机械检查全绿 ≠ 可以交付。** 幻灯片 12 道门全绿之后，**用眼睛看又打出了 3 个 REVISE** —— 角标压住图的标题、公式字号不一致、宽图被挤到读不了。**这三个在语法上、在包围盒上、在字号上全都合法。**
    > **机械检查负责「不可能撒谎」；眼睛负责「看得见」。两个都要。**

## Skill 3 的核心：**数字不许手打，只许写指针**

```json
✗  "text": "有限长磁体给出的指数是 3.44"
✓  "text": "有限长磁体给出的指数是 {{assertions.AS-8.measured|.2f}}"
```

第二种写法里，**3.44 这个数字在 `deck.json` 里根本不存在** —— 渲染时从 `results.json` 取值代入。
于是仿真一重跑，幻灯片**自动跟着变**，**而不是变成一个谎**。

> **弱的做法是「写完之后核对一遍」。你不会核的。**
> **强的做法是「根本没有那个入口」。**

阈值这类**只以散文形式存在**的数字（「若 `|k-4| > 0.3` 则降级」），走**行内引文**：
`{{|k-4| > 0.3 @ risky_checks.A-1.quoted_pass_criterion}}` —— 写得出那个数，
但**必须一字不差地是契约原文的子串**。**编的数字不可能是原文的子串。**

其余三条硬约束（全部有机械门）：

- **被证伪 / 被降级的断言必须以 `verdict` 块出现**（`FALSIFIED-DROPPED`）——
  它的期望 / 实测 / 判定全从 `results.json` 长出来，**你无法在展示它的同时把它说成一次成功**。
- **`status` 不是 `PASS` 时必须有 `boundary` 一节**（`STATUS-HIDDEN`）。
  **这是加分项，不是要藏的东西。**
- **必须有 `disclosure` 块**（`DISCLOSURE-MISSING`）—— 它**不接受任何参数**，
  文字由渲染器写死。**凡是可以被措辞软化的底线，迟早会被措辞软化。**

## 工程约定

- 输出：中文正文 + 英文物理术语 + LaTeX 公式。**但图上的文字一律英文**（IYPT 是英文赛事，图直接进英文 PPT；而且 DejaVu Sans 没有中文字形，中文会渲染成豆腐块）。
- 联网查文献允许，但必须给引用，并严格区分"文献结论"与"自己推导"。
  **红线：不许去查"答案应该是多少"然后回来调代码去凑**——那是拟合，不是计算。
- 改完东西，全跑一遍（**零 ERROR**）：
  ```bash
  python skills/iypt-analysis/scripts/check_analysis.py examples/magnetic-brake
  python skills/iypt-simulation/scripts/check_sim.py   examples/magnetic-brake
  python skills/iypt-slides/scripts/check_slides.py    examples/magnetic-brake

  python examples/magnetic-brake/02-sim/code/run_all.py       # 仿真一键复现
  python examples/magnetic-brake/02-sim/code/smoke_test.py    # 4 个注入全被抓到
  python examples/magnetic-brake/03-slides/build.py           # 幻灯片一键重渲
  python examples/magnetic-brake/03-slides/smoke_test.py      # 9 个注入全被抓到
  ```
  **改了 `skills/iypt-slides/templates/` 里的东西，记得同步到 `examples/magnetic-brake/03-slides/`**
  （和 `figkit.py` 一样是「拷进工作区」的模板）。
- **然后用眼睛看**：`Read examples/magnetic-brake/03-slides/png/S-*.png`。
  **机械检查全绿 ≠ 可以交付**（见「已经吃过的亏」第 10 条）。
- 环境：Python 3.12 + numpy/scipy/matplotlib，node v24。**没有 MATLAB/Octave**（所以 MATLAB 只做"带自检的移植"，`matlab_port.verified` 必须是 `false`）。**没有 ffmpeg**（动画走自包含 HTML）。**没有 python-pptx / Marp / LibreOffice** —— 幻灯片走 **HTML → headless Edge/Chrome → 每页 PNG + PDF**（`render_html.py`）。选它的理由不是省事：**只有渲得出 PNG，Skill 4 才能「真的打开用眼睛看」，设计审查回路才成立**；python-pptx 直出 .pptx 会在这里断掉。
- Windows 注意：Python 从 stdin 读中文源码会按 GBK 解码而乱码——脚本写成文件再跑；控制台输出中文要 `sys.stdout.reconfigure(encoding="utf-8")`。
