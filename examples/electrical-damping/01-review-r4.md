# 审稿报告 · 第 4 轮

**判定**：**MAJOR**
**审稿人视角**：Opponent
**被审对象**：`01-analysis.md`（r4，1457 行）、`handoff/model-spec.json`（r4）、`01-criteria/criterion_matrix.py` + `matrix.json`、`00-problem.md`
**归档链**：`model-spec-r1/r2/r3.json` → `model-spec.json` —— 四代逐版 diff 已做（P16）
**方法**：P1–P15 + P17 + P18 逐条执行；P16 必查（归档链存在）；第 1.5 步（复审传播）先做。
所有几何、四层模型的 `|G'(0)|`（椭圆积分 + Richardson 有限差分，**独立于代码的样条法**）、`c₂`、`γ` 基准、
δ 扫描、ε* —— 全部用 numpy/scipy **实算**。`check_analysis.py` 报 **0 ERROR** —— **第四轮了。**

---

## 一句话结论

> **物理是硬的（我攻不动），r3 的两把瞎锁（STALE-VALUE、双向表）也真的补上了。**
> **塌的是 r4 为补第二把锁而新造的第三把 —— `robustness_scan` 的 δ 扫描：**
> **它报「判据的有效窗口 δ_max = 0.17 mm、距离悬崖 1.7 倍」，而用它自己的代码在 δ = 0.14 mm 上跑，正确模型就被判死（P3b 偏 +10.9% > 10%）。真实悬崖 ≈ 0.13 mm，安全倍数 ≈ 1.3。**

**为什么第三把锁也瞎？** 因为 δ 的扫描网格 `[0, 0.05, 0.10, 0.17, 0.25] mm` 是**作者挑的**，它**恰好跳过了 0.11–0.16 mm** —— 边界就藏在那段里。**这是 P18 的原样重演，升了一层楼：r3 的双向表把「错模型的幅度」调成了作者挑的；r4 把「robustness 扫描的采样点」调成了作者挑的。同一个病，换了个维度。**

外加四条 MINOR，**全是「全局重标定/术语修订没传播到散文」**（贯穿四轮的 #1 复发 bug 的第 4 代）——
**其中最刺眼的一条：`§11` 描述「我怎么消灭了三代不动的错值 2.5978」的那一段里，自己手打了一个消不掉的幽灵值 `2.3454`**（契约里是正确的 `2.3554`，我实算确认）。**又一次在写规矩的那一句话里违反了那条规矩。**

---

## 命中的洞

### H1 · [MAJOR] `robustness_scan` 的 δ 网格跳过了真实边界 —— δ_max = 0.17 mm 是虚报的，真值 ≈ 0.13 mm — 命中 P18④ + P17③ + P3

**位置**（逐字抄写）。`01-criteria/criterion_matrix.py` 第 422 行：

```python
DELTAS = [0.0, 0.05e-3, 0.10e-3, 0.17e-3, 0.25e-3]
```

`matrix.json` 的 `robustness_scan`（逐字）：

> `"delta_max": 0.00017,`
> `"verdict": "判据的有效窗口：**δ < 0.17 mm**。协议必须把 z_off 定到 **0.10 mm** 以内。（§9 的视频噪声是 ±0.1 mm —— 距离悬崖 1.7 倍。）"`

`01-analysis.md` §8.5 ②（L1054）与 §9「需要的精度」（L1263）三处一致：

> **⟹ 判据的有效窗口：$\delta_{\max} = 0.17$ mm。**
> **而 §9 的视频噪声预算是 ±0.1 mm —— 距离悬崖只有 1.7 倍。**

**为什么崩**。那张 δ 表只测了 5 个点，从 `0.10`（全过）**直接跳到** `0.17`（判死），中间 6 个整数十分之一毫米一个没测。我拿**作者自己的 `criterion_matrix` 代码**（同一个 `G_true` 样条、同一个 `fit_bernoulli`、同一个 `GAMMA_OC`、P3b 同一个 `tol = 0.10`），把 δ 加密：

| δ (mm) | **P3b** `Γ/γ_oc − 1`（容差 0.10） | P3a 斜率偏差（容差 0.15） |
|---|---|---|
| 0.10 | +5.5% ✓ | −5.2% ✓ |
| 0.12 | +8.0% ✓ | −7.3% ✓ |
| **0.13**（内插过零点） | **≈ +10.0% —— 悬崖** | −8.5% ✓ |
| **0.14** | **+10.9% ✗✗ 判死正确模型** | −9.7% ✓ |
| 0.16 | +14.2% ✗✗ | −12.4% ✓ |
| 0.17（**代码测的下一个点**）| +16.1% ✗✗ | −13.7% ✓ |

**⇒ P3b 判死正确模型的真实边界 ≈ 0.13 mm，不是 0.17 mm。虚报了 ~30%。**
**⇒ 而「距离悬崖 1.7 倍」（= 0.17/0.10）的真值是 0.13/0.10 ≈ 1.3 倍。**

**影响**。δ_max 这个数是 **r4 的招牌新功能** —— 它是 r3 审稿 H3 逼出来的「判据的有效窗口必须是一个写下来的数」。
- **这个「写下来的数」写错了**，而且错在 P18 反复警告的那个方向上：**采样是作者挑的**。
  §9 自己把「距离悬崖 1.7 倍」当成一条 ★★ 级的风险警告；真值 1.3 倍意味着风险比文档承认的**更贴脸** ——
  视频噪声 ±0.1 mm 的一次涨落就能把残余偏心推到 0.13 mm 的悬崖上，**在那里正确模型被自己的验收判据判死**。
- 主结论**没被推翻**：协议要求「z_off < 0.10 mm」在 0.10 mm 上确实还过（+5.5%），这一条是安全的、保守的。
  **崩的是它旁边那个安全裕度的数** —— 而那正是 `教训 11`（门的容差必须在扫描端点上定）+ `P17③` 的原话：
  **「一个漂亮的论证（δ 扫描这个动作是对的），恰恰掩护了它旁边那个没在足够细的网格上算过的数。」**
- **这就是这道题复发的元模式**：r1 死于判据在极值点失明 → 补 ν；r2 三把新锁瞎 → 补双向表；
  r3 STALE-VALUE 对「值没变」失明 + 双向表是自评 → 补 SPEC-SELFCONTRADICT + P18 五维度；
  **r4 补的 δ 扫描，采样网格太粗，边界报虚了。每一轮，新锁都在同一个地方瞎：只在作者选的那几个点上验过。**

**Opponent 的杀招（Reporter 答不上来）**：
> 「你写判据的有效窗口是 δ < 0.17 mm、安全 1.7 倍。我用你自己的代码在 δ = 0.14 mm 跑正确模型 —— P3b 偏 +10.9%，判死。真值是 0.13 mm、1.3 倍。你的 δ 网格恰好跳过了 0.11–0.16 mm。」

**可否修**（三行）：把 δ 扫描改成二分找边界（像 `eps_star` 那样），或至少把网格加密到 0.02 mm；
把 `delta_max ≈ 0.13 mm`、`安全倍数 ≈ 1.3` 传播到 §8.5、§9、`matrix.json`、`figures[F-3]`。
**并把这条钉进机械门**：`CRIT-NO-ROBUSTNESS` 现在只查「δ 扫描存在」，不查「边界是二分定出来的还是网格撞出来的」——
**又一个查布尔值的门。**（回填建议见末节。）

---

### H2 · [MINOR] `§11` 消灭错值的那一段，自己带着一个消不掉的幽灵值 `2.3454` — 命中 P1（零号规则的镜像）+ P16（传播）

**位置**（逐字抄写）。`01-analysis.md` §11「★★★ r4 的核心教训」（L1338–1339）：

> **实测**：`targets[\gamma].baseline_value = 2.5978` 在 **r1 / r2 / r3 三代一字未动**，
> 而它自己的公式算出来是 **2.3454**（偏 +10.8%）。

以及 H1 修订行（L1365）：「`targets[γ]` = 2.5978 三代未动（**真值 2.3454**）」。

**而同一份 §11 的「r4 改动的结论」表（L1382）写的是**：

> | $\gamma$（基准点） | 2.5978 | **2.3554** | r1 的错值三代未动 |

**⇒ 同一份文档、同一个量、同一节，两个值：`2.3454`（两处）与 `2.3554`（一处 + 契约）。**

**为什么崩**。我按契约 `targets[γ].closed_form`（逐字）：`"gamma_oc + G_max**2/(2*M_eff*(R + R_c))"`，
代 `G_max = 0.8482479`（契约 `targets[G_max]`）、`M_eff`、`R = 20`、`R_c = 3.7096` 实算：

$$\gamma = 0.0413 + \frac{0.848248^2}{2\times0.0065667\times23.7096} = \mathbf{2.35554}$$

- **契约 `baseline_value = 2.3554174` 是对的**（吻合我的 2.35554 到 0.005%）—— `SPEC-SELFCONTRADICT` 正确放行。
- **§11 散文里的 `2.3454` 是错的**（偏 0.43%），而且它被归给「它自己的公式算出来是」—— **公式算出来根本不是这个数**。
- **「偏 +10.8%」也是错的**：`2.5978/2.3554 − 1 = +10.3%`，不是 +10.8%（+10.8% 对应幽灵值 2.3454）。
- **自洽性佐证**：§4 的 $\Pi_2 = \zeta = 0.1228$（契约 0.1227609，我复算一致）+ $\Pi_3 = \gamma_{\rm oc}/\omega_0 = 0.00219$
  ⟹ $\Pi_1 = \gamma/\omega_0 = 0.1250$ ⟹ $\gamma = 2.356$ —— **和 2.3554 自洽，和 2.3454 不自洽。**
  （顺带：§4 表里 $\Pi_1$ 的基准值写的是 `0.124`，应是 `0.125` —— 又一处随手的旧值。）

**这个 `2.3454` 从哪来**：它是 **r3 审稿 L68 手算的那个数**（`0.0413 + 0.71673/0.31106 = 2.3454`，用的是 r3 当时的 $G$）。
r4 重标定后 $G_{\max}$、几何都变了，真值成了 2.3554，**但 §11 的叙述把 r3 报告里的 2.3454 逐字搬了过来，没重算。**

> **零号规则通常说：审稿人不许拿自己的推导替换作者的字。**
> **r3-H4 抓到过它的镜像：作者拿记忆里的 `+27.7%` 替换了审稿人的字。**
> **r4 又犯了一次同一个镜像**：作者把 **r3 审稿手算的 `2.3454`** 当成「公式算出来的真值」搬进了正文 ——
> **而公式现在算出来是 2.3554。** **编出来/记出来的数，不可能是自己公式的输出。**

**为什么机械门看不见它**：`SPEC-SELFCONTRADICT` 查的是**契约**的 `baseline_value` vs `closed_form`（都对，2.3554 ↔ 2.3554）。
`REVIEW-CITE-GHOST` 查的是「归给 r{n} 审稿的数能不能在 `01-review-r{n}.md` 里 grep 到」—— 而 `2.3454` **确实**在 `01-review-r3.md` 里（L54/68），所以门放行。
**但 §11 说的是「它自己的公式算出来是 2.3454」—— 没有任何门去把这句话里的数代进那个公式验一遍。**
**散文里的、归给「公式」而非「审稿」的幽灵数，四道 SPEC/REVIEW 门一个都抓不到。**（同 H1：新锁的盲区。）

**影响**：契约（Skill 2 读的）是对的，主结论不变。纯散文错误 ⟹ MINOR。**但它是四轮里最讽刺的一处**：出现在标题为「我为查错值造的门自己也是瞎的」那一节里。

**可否修**：§11 的两处 `2.3454` → `2.3554`，`+10.8%` → `+10.3%`；§4 的 $\Pi_1$ `0.124` → `0.125`。

---

### H3 · [MINOR] §9 器材清单还在命令「买 20 股利兹线（S-8）」—— 与同节 §6.2/第 8 步直接矛盾，且 S-8 这个 id 已删 — 命中 P16（传播）

**位置**（逐字抄写）。`01-analysis.md` §9「器材」（L1134）：

> - **★ 可选：20 股利兹线绕的第二个线圈**（S-8）—— 若要真的验收 P3′ 的渐近指数，**必须换它**（§6.2）

**而同一节 §9 第 8 步（L1235）写的是**：

> **（★ 不是利兹线 —— 那条算错了，见 §6.2。）**

**以及 §6.2（L811–824）整整一段**证明利兹线是错的杠杆（束粗 22% ⟹ β 掉 23% ⟹ 窗口 0.998 → 0.954，反而更差）。

**⇒ §9 的器材清单让你去买利兹线，§9 的操作步骤和 §6.2 告诉你利兹线是错的。同一节自相矛盾。**
**而且 `S-8` 这个 id 在 `00-problem.md` 里早已改名 `S-8′`（∅0.20 mm 实心线）——器材清单指向一个被删掉的设定。**

**这正是 r3-H2 的复发**。r3-H2 判 MAJOR，是因为当时**契约**的 `symbols[r_w]` 和 `A-6b.impact_if_false` 都在命令买利兹线。
**r4 把契约修干净了**（我 grep 确认：`model-spec.json` 里唯一的「利兹」在 `symbols[r_w]`，且是「**不是**利兹线」的正解）——
**但漏了 `01-analysis.md` §9 的器材清单这一处。** 契约（Skill 2 读的）干净 ⟹ 降为 MINOR，但**给人做实验的那份清单**里，它是错的、且自相矛盾、且指向死 id。

**可否修**（一行）：L1134 的「20 股利兹线绕的第二个线圈（S-8）」→「$\varnothing0.20$ mm 实心漆包线绕的第二个线圈（S-8′）」。

---

### H4 · [MINOR] 「机械本底主导」在两个文档里还活着 —— 契约已改「开路本底」 — 命中 P16（传播）

**位置**（逐字抄写）：

- `01-analysis.md` §0.5（L61）：「衰减包络的形态 —— **指数 / 幂律 $t^{-1/2}$ / 机械本底主导**」
- `00-problem.md` §3.3（L75）：「**指数** / **幂律 $A\propto t^{-1/2}$** / **机械本底主导**」

**而契约 `essence.system_type` 与 `variables.dependent`（逐字）已改成「开路本底主导」。**

**这是 r3-H1 #9（「机械本底」→「开路本底」）的复发**。整个 r2/r3 的核心物理就是：**它不是「机械」本底 —— 开路读数里必然含 24% 的导线涡流**，所以叫**开路本底**。契约的 9 处改了，`01-analysis.md` §0.5 和 `00-problem.md` §3.3 这两处 mode 标签没改。MINOR（术语，不改物理），但它是这道题被审了四轮的那个招牌区分，不该留旧词。

**可否修**：两处「机械本底主导」→「开路本底主导」。

---

### H5 · [MINOR] A-1 的 `|G'(0)|` 数对（103.44 → 105.52）是重标定**之前**的值 — 命中 P16（传播）

**位置**（逐字抄写）。`assumptions[A-1].impact_if_false`（契约）与 §3 A-1 `impact_if_false`（L206–207）：

> **但 $\lvert G'(0)\rvert$（中心斜率）偏 $+2.0\%$（**103.44 → 105.52**，Model-2）**

**为什么是旧值**。我用椭圆积分 + Richardson 有限差分（**独立于代码的样条二阶导法**）实算，网格与内层求积都推到收敛：

| 量 | 我的独立复算 | 代码/契约现值 | A-1 impact_if_false 写的 |
|---|---|---|---|
| Model-0 `|G'(0)|` | **103.55** | 103.546 ✓ | **103.44**（r3 旧几何 a=10.80） |
| Model-2 `|G'(0)|` | **105.77** | 105.747 ✓ | **105.52**（r3 旧几何） |

`103.44/105.52` 是 **r3 那一代**（六方嵌套重标定**之前**，$a = 10.80$ mm）的数；r4 重标定后 $a = 10.784$ mm ⟹ 现值 `103.55/105.77`。
**A-1 的这个数对没跟着重标定走** —— 它是第 4 代 stale，正是本题贯穿四轮的 #1 bug。
偏差极小（0.1% / 0.24%），不改 +2.0% 的结论，故 MINOR。（同类的还有 A-1 里的 `|G|_max` Model-2 = 0.8145，真值 0.8159。）

**可否修**：A-1 的 `103.44 → 105.52` → `103.55 → 105.77`。

---

## 我独立复算的关键量（vs 契约 / 代码）

| 量 | 我的独立方法 | 我的值 | 契约 / 代码 | 判 |
|---|---|---|---|---|
| 六方嵌套 $a$ | 层间距 $d\sqrt3/2$ | **10.784 mm** | 10.784 | ✓ |
| $R_c$ | $\rho\ell/(\pi r_w^2)$ | **3.710 Ω** | 3.7096 | ✓ |
| Model-0 `|G'(0)|` | 解析式 (7) | **103.55** | 103.546 | ✓ |
| Model-0 `|G|_max` / $z_{\rm pk}$ | 数值求极值 | **0.8482 / 10.453 mm** | 0.8482 / 10.4533 | ✓ |
| **Model-2 `|G'(0)|`** | **椭圆积分 + Richardson FD（非样条）** | **105.77** | **105.747**（样条二阶导） | ✓ **代码样条法误差仅 0.03% —— 收敛、可信** |
| Model-2 `|G|_max` / $z_{\rm pk}$ | 样条 | 0.8159 / 10.60 mm | 0.8159 / 10.598 | ✓ |
| $c_2$ (Model-0 / Model-2) | $|G'(0)|^2/[2M_{\rm eff}(R{+}R_c)]$ | **0.03448 / 0.03598** | 0.0345832 / 0.03597 | ✓ |
| **$\gamma$ 基准** | closed_form 实算 | **2.35554** | **契约 2.3554174 ✓；§11 散文 2.3454 ✗（H2）** | 契约对，散文错 |
| **$\delta_{\max}$（P3b 判死边界）** | **加密 δ 扫描（作者代码）** | **≈ 0.13 mm** | **0.17 mm（H1）** | ✗✗ **虚报 30%** |
| $\varepsilon^*$ bug-C / bug-E | 复跑 `eps_star` | **13% / 2%** | 0.1312 / 0.0187 | ✓ |

> **⇒ 关于「Model-2 的 G(z) 数值实现对不对」这个攻击点**：**我攻了，没打穿。**
> 代码用 0.2 mm 网格的三次样条二阶导取 `|G'(0)|`，我担心它不收敛 —— 实测它 = 105.75，
> 我用完全独立的方法（更细求积 + Richardson 外推）得 105.77，**误差 0.03%。样条法在这里是收敛且准确的。**
> `+2.15%`（Model-2 高于 Model-0）是真的，方向也对（有限磁体压低峰值、抬高中心斜率）。

---

## 18 条模式的逐条结果（P1–P15 + P17 + P18；被审稿修订过，加查 P16）

| 模式 | 结果 | 我做了什么检查 |
|---|---|---|
| **P1 量纲/单位** | **命中 → H2**（幽灵值） | 逐式量纲代数 (1)–(27) 全齐次；$[c_2]=\mathrm{s^{-1}m^{-2}}$、$[Q]=1$、$[\Gamma]=\mathrm{s^{-1}}$、$[\beta]=\mathrm{N\,s/m^3}$ 逐个核 ✓。**然后逐个数字重算**（见上表）：几何、Model-0/2 的 `|G'(0)|`/`|G|_max`/`z_pk`、$c_2$、$\varepsilon^*$ 全部复现。**塌的是 §11 的 `2.3454`（H2，散文幽灵，契约是对的 2.3554）。** |
| **P2 口头忽略** | 未命中 | 逐个「忽略」要数值比：电感 1.26%/0.023%（两端）、磁体内涡流 $9\times10^{-9}$（功率比）、导线涡流 $\gamma_{\rm eddy}=0.0097$（功率比，**并入 $\gamma_{\rm oc}$，不是忽略**）、空气阻力 $\le1.3\%$、自热、磁滞。**r1 的长度比 non sequitur 已彻底改成功率比。全部闭合。** |
| **P3 忽略/门只在扫描一端成立** | **命中 → H1** | §5.1 端点表覆盖每一行机制 ✓。**新命中**：δ 扫描的网格太粗（5 点），跳过 0.11–0.16 mm ⟹ 判据的有效窗口 δ_max 报成 0.17 mm，真值 ≈ 0.13 mm。**这正是 `教训 11`（门的容差/窗口必须在扫描端点上定）的第四次发作。** |
| **P4 力/能量双重计数** | 未命中 | 力项（$-ku$、$-2M_{\rm eff}\gamma_{\rm oc}\dot z$、$IG$）与能量项互补；能量法与力法在 (20) 严格一致（我重推 $\langle\cos^2\sin^2\rangle=1/8$）✓ |
| **P5 非惯性系漏项** | 未命中 | 全文实验室系；重力只平移平衡点，不进 (13)/(26) ✓ |
| **P6 边界条件不物理** | 未命中 | (26) 是 ODE；(27) 的场积分边界（磁体表面、绕组体积）显式；均匀磁化面电流 ✓ |
| **P7 线性化越界** | 未命中 | A-2 的 $\nu$ **无关阶数**（r1 的极值点失明已修，我验它在 $A_0=8$ mm 看得见 31%）✓；抛物线拟合区间 $\pm4$ mm **由 $G$ 的非线性 <1% 算出**，不是随手画 ✓；A-8（弱阻尼平均）有编号有判据 $\zeta_{\rm eff}\ll1$ ✓ |
| **P8 公式超出适用域** | 未命中 | 点偶极子（$a/\max=1.08$，作者承认 RISKY 并量化）；Ferreira/Dowell 涡流 $p=\frac18\sigma r_w^2(\dot B_\perp)^2$（$\delta\gg r_w$ ✓ **作为适用条件，不是可忽略判据**）；Stokes vs 牛顿（$Re=13$–$101$，二次阻力）✓。**S-8′ 标度律 $\beta\propto Nd^2$ 精确不变 —— 我复核 $1565\times0.2^2/(400\times0.4^2)=0.978$ ✓** |
| **P9 时间尺度矛盾** | 未命中 | $L/(R{+}R_c)=0.67$ ms $\ll 1/\omega_0=53$ ms（刚性比 80）；$\delta_{\rm skin}\gg r_w$；A-8 的 $\zeta_{\rm eff}$ ✓ |
| **P10 耗散符号/热二** | 未命中（**零号规则：逐字抄了 (26).latex**） | `M_{\rm eff}\dot v = -k(z-z_0) - 2M_{\rm eff}\gamma_{oc}v + IG`；`L\dot I=-(R+R_c)I-Gv`。**两处质量都是 $M_{\rm eff}$（r2 的「LHS 用 M」已修）。** 代 $I=-Gv/R_{\rm tot}$ ⟹ 力 $\propto-v$（Lenz）；$\dot E=-[2M_{\rm eff}\gamma_{\rm oc}+b]\dot z^2\le0$ ✓ **无自发增能** |
| **P11 循环论证** | 未命中 | $k=Mg/\Delta x$（$\Delta x$ = 挂磁体前后之差 ⟹ 弹簧自重精确抵消）⟹ $M_{\rm eff}=k/\omega_0^2=6.557$ g $\ne M$ ✓ **r2 的循环被真正打断** |
| **P12 自由参数掩盖物理** | 未命中 | $N_f=0$：$G'(0)$←高斯计、$R_c$←万用表、$\gamma_{\rm oc}$←开路实测、$M_{\rm eff}$←$k/\omega_0^2$ ✓ |
| **P13 符号未定义/复用** | MINOR（未升级） | `check_analysis` 0 ERROR；`gamma0` 孤儿 **已清干净**（我 grep 契约确认）✓。**残留**：$Q$ 既是弹簧品质因数又是 (23a) 的非线性度（符号表两条都在，下标/上下文可区分，r3 已记）—— MINOR，判定不变 |
| **P14 结论对 RISKY 敏感却没标注** | 未命中 | A-1/A-2（RISKY）、A-3/5/6b/7/8（LOAD-BEARING）逐条有 `impact_if_false` + `risky_assumption_checks` + **离散退化特征**；A-8 的「误差 $\propto\zeta_{\rm eff}^2$，log-log 斜率 $2.0\pm0.3$」仍是教科书级预注册 ✓ |
| **P15 用「文献说」替代推导** | 未命中 | §10 七条逐核；Neumann 只借名字（(1) 我独立重推）；Ferreira/Dowell 形式对、适用条件核过、体积分自己做；$B_r$ 主动标 ±5% ⟹ 必须实测 $G$ ✓ |
| **P16 事后合理化（HARKing）** | 未命中 HARKing（**但传播命中 → H2/H3/H4/H5**） | 四代归档逐版 diff。r4 的每一处（γ 重标定、六方嵌套、δ 扫描、ε*、三道 SPEC 门）**都由 r3 审稿驱动，不是照数值答案倒推**（`02-sim/` 不存在）。A-2 的预注册应对（$\gamma_{\rm oc}>0.045\Rightarrow\ell_c$ 减到 12 mm）已补 ✓。**不判 HARKing。** 但**修订传播不干净** ⟹ 四条 MINOR |
| **P17 判据界错了量** | **命中 → H1** | ① 量纲：A-6a/A-6b 都是**功率比** ✓；② 分子在基准点恒零？$\nu$ 无关阶数、P1a/b/c/P3a/b 全无导数 ✓；③ **扫描端点/门的容差** —— **δ_max 没在足够细的扫描上定（H1），报虚了 30%**；④ 双向验证见 P18 |
| **P18 双向表是自评** | **命中 → H1**（另五维度未命中） | **① 容差有来源** `tolerance_source` ✓（r3 的裸数字已修）；**② ε* 扫幅度** ✓（bug-C 13%/bug-E 2%，我复现，且诚实指出标称 $B_r$ 的 10% < 13% 抓不到）；**③ 正确模型 = Model-2** ✓（我验 `G_true` 确是有限磁体+厚绕组）；**④ 不误杀扫 δ —— 命中 H1**（网格太粗，边界虚报）；**⑤ CAUGHT vs DEGENERATE** ✓（naive-A/B 只由 P1a 的 `gamma_early` 真抓到，退化不计入）；**⑥ bug-E 的 `why` 只留磁体倾斜** ✓。**六维度里五个是真的、一个（④）又瞎了。** |

---

## 没打穿的地方（说清楚我攻击了什么、为什么它扛住了）

**r4 的物理和大半的机械化是硬的。审稿人的义务是说清哪些子弹打空了：**

- **★ Model-2 的 `|G'(0)|` 数值实现** —— 我最想打穿的点（样条二阶导 + 0.2 mm 网格 + 粗求积）。
  用完全独立的方法（椭圆积分单匝环场 + 高阶求积 + Richardson 外推）复算 = **105.77，代码 105.747，误差 0.03%。收敛、准确。**
- **★ γ 基准的契约值** —— `SPEC-SELFCONTRADICT` 该抓的它抓住了：`baseline_value = 2.3554174` 与自己的 `closed_form` 一致（我实算 2.35554）。**这道新门在契约上是好的**（塌的是它照不到的散文，H2）。
- **★ 双向表的五个维度**（容差来源、ε*、Model-2、CAUGHT/DEGENERATE、bug-E 的 why）—— 逐个复跑 `criterion_matrix.py`，30 格一格不差，**作者没编数字**；ε* 我用二分独立复现 13%/2%。
- **★ (23a) 的 Bernoulli 代数、$G(0)=0$ 的对称性、S-8′ 的标度律、$M_{\rm eff}=k/\omega_0^2$ 的非循环** —— r3 验过，我抽查一致。
- **P4/P5/P6/P9/P10/P11/P12/P14/P15** —— 全部打空。
- **P16 HARKing** —— 不判：驱动是审稿不是数值；预注册在场；`02-sim/` 不存在。

---

## UNCLEAR（我无法判断的）

1. **δ_max 加密后的确切值**：我把边界钉在 **0.12 mm（+8.0%，过）与 0.14 mm（+10.9%，判死）之间 ≈ 0.13 mm**（P3b，容差 0.10）。
   这已足够判 H1（0.17 mm 虚报）。但**精确到小数点后两位**需要作者用二分重跑 —— 我给的是「悬崖在 0.13 mm 附近，绝不在 0.17 mm」。
2. **有噪声下 $\sigma(z_{\rm off})$**（r3 也判 UNCLEAR）：无噪声仿真给 ~0.03 mm，但 §9 说视频噪声 ±0.1 mm、$\Gamma$ 测到 2%。
   带上这些，$\sigma(z_{\rm off})$ 是多少？**若它 $\gtrsim 0.13$ mm，P3a/P3b 的验收就悬空了。** H1 让这个问题更紧迫（悬崖比文档说的近）——**作者必须把这个数算出来。**
3. **$\gamma_{\rm eddy}$ 的绝对大小仍有 ~2× 模型不确定度**（孤立圆导线 + 局部均匀 $\dot B_\perp$，未含邻匝互屏蔽 / 螺旋）。
   与 r3 同。它改不了「涡流是 $\gamma_{\rm oc}$ 的 24%」这个定性事实，但 $\gamma_{\rm oc}$ 的 24% 那一份的**精确值**要 3-D 涡流有限元才能定死。

---

## ★ 给 skill 的回填建议（**任何题都会踩的坑**）

### 1. ★★★ `robustness_scan` 的 δ 边界必须**二分定出**，不能靠网格撞

> **实测（本轮）**：`criterion_matrix.py` 的 `DELTAS = [0, 0.05, 0.10, 0.17, 0.25] mm` —— 5 个作者挑的点。
> 正确模型在 `0.10` 过、`0.17` 判死，中间没测。真实边界 ≈ **0.13 mm**（我用它自己的代码加密验证）。
> **⇒ `delta_max` 报成 0.17，虚高 30%；派生的「安全 1.7 倍」真值 1.3 倍。**
>
> **这是 P18 ④ 的原样重演，升了一层楼**：
> **r3 教训是「错模型的**幅度**是作者挑的」⟹ 补了 `eps_star`（二分）。**
> **r4 的教训是「robustness 扫描的**采样点**是作者挑的」—— 而 `eps_star` 已经会二分了，`delta` 却还在用固定网格。**
> **⇒ `delta_max` 必须和 `eps_star` 一样二分找边界；`CRIT-NO-ROBUSTNESS` 不能只查「δ 扫描存在」（布尔值），
> 要查「边界是二分定出来的」——否则它又是一个查布尔值的门。**

### 2. ★★ 散文里归给「公式/计算」的数，也要被代回那个公式验一遍

> 本轮：§11 写「`targets[γ]` 自己的公式算出来是 **2.3454**」，而公式实际算出来是 **2.3554**（契约里就是 2.3554）。
> `SPEC-SELFCONTRADICT` 查的是契约字段（对），`REVIEW-CITE-GHOST` 查的是「归给审稿的数」（2.3454 确实在 r3 报告里，放行）。
> **没有门去查「散文里归给『公式』的数，代进那个公式对不对」。**
> **⇒ 零号规则的镜像第三次发作**（r3 是 +27.7% 归给审稿；r4 是 2.3454 归给「公式」）。
> **⇒ 凡是正文里写「X 的公式/闭式算出来是 N」的，N 必须能由那个 `closed_form` + `parameters` 复算到（容差 1%）。**

### 3. ★ 「术语/设定重命名」必须 grep 全工作区，不能只改契约

> 本轮：契约把「机械本底→开路本底」「利兹线 S-8→实心线 S-8′」都改干净了，
> **但 `01-analysis.md` §9 器材清单还在「买 20 股利兹线（S-8）」，§0.5 和 `00-problem.md` §3.3 还写「机械本底主导」。**
> **⇒ 这是 r3-H1/H2 的复发，只是这次残留在散文而非契约。**
> **「改契约」和「改分析正文/问题书」是两件事 —— 重命名后必须拿旧词 grep 整个工作区，`.md` 和 `.json` 一起。**

### 4. ★ 全局重标定后，每一个「派生数对」都要重算，不能留上一代的

> 本轮：`A-1.impact_if_false` 的「103.44 → 105.52」是六方嵌套重标定**之前**的值（现值 103.55 → 105.77）。
> 偏差小（不改结论），但它是第 4 代 stale。**重标定改的是 $a$ ⟹ 每一个 $\propto a^{\pm n}$ 的数都得跟着走**，
> 包括藏在 `impact_if_false` 散文里的「示例数对」。
