# 设计审查 · 02-sim 图集 · 第 2 轮（复验）

**判定**：`APPROVE`（r1 的 3 处 REVISE 全部真实解决，无回归；fresh agent 逐像素复验 + 独立复算）

r1（`02-design-review-r1.md`）打了 3 个 REVISE，其余 APPROVE + 若干 MINOR。本轮只复验这 3 处，
由一个无先前上下文的 fresh agent 执行：逐张 Read 真实像素、H1 另用 browser-eyes 亲自驱动 HTML 并**独立复算** eq.15。

| 项 | r1 问题 | r2 复验 |
|---|---|---|
| **H1** · 交互页 + I-1（D12，最致命） | `damping-explorer.html` 把只在 \|z₀\|≤4 mm 拟合的 Γ 抛物线 `γ_oc+c₂z₀²` 外推到整个滑块域 —— z₀=10.5 读出 Γ=25.39，而本次运行注册真值 ≈13.7（偏高 ~85%）；caption 声称「不可能与 results.json 打架」在 \|z₀\|>4 mm 处被证伪 | **PASS** —— 改为按契约 **eq.15** `Γ(z₀;R)=γ_oc+G(z₀)²/[2·M_eff·(R+R_c)]`，查烘焙的 **eq.27 G(z₀) 表**（0.25mm 网格线性插值），`c₂(R)=c₂(0)·R_c/(R+R_c)`。浏览器实测 z₀=10.5：Γ=**13.6758**(R=0)、**2.1746**(R=20，= `targets[γ]` 2.1741)。agent 从页面 `DATA.G_table` 复算 eq.15 逐位吻合。I-1 静帧紫线用同一路径重建（z₀ 10.45→10.5，可达滑块步）；footer/caption 已如实命名 eqs.(15)+(23a) 并注明 r1 发现 |
| **H2** · F-1（D1/D4 遮挡） | 图例（两行长标签+半透明白底）压住整个负峰 z∈[+5,+17]，双峰里的一个被埋，AS-19 的 Model-0/2 谷底分离看不见 | **PASS** —— 图例两行换行移到**左下**（奇函数在该处走上半支 ⟹ 底下确认无数据）；负峰谷底 + 全部数据点 + z_pk 虚线完全可见。「z_pk=10.60mm」贴顶、「G(0)=0 exact」居中带箭头 —— 上下分层，不叠、不压数据/图例 |
| **H3** · F-3(b)（D1 遮挡） | `γ_oc=0.0413` 白底直标骑在 y=0.0413 结论线上，A₀=5/8 两个数据点只剩下半圆 —— 而该面板全部证据就是「四点全压 γ_oc 线」（AS-15） | **PASS** —— 直标上移到 y≈0.05，离开 ±10% 带；A₀=2/3/5/8 四点均为完整实心圆 |

**无回归**：F-1 右面板、F-3 (a)/(c)/(d)、I-1 regime map 与两条幂律曲线、SIMULATION 戳、绿 PASS 框、HTML console 零错误 —— 均未变坏。

> **本轮的价值**：H1 是一个**数值**错误（读数偏 85%），不是排版问题 —— 它在语法/包围盒/console 上全都合法，
> 只有「真的在浏览器里把读数对着 results.json 溯源」才抓得到。这正是 fresh 设计审查（对抗式、无作者上下文）
> 存在的理由：作者的 browser-eyes 只查了 console 干净，没对读数做物理溯源。
